#include<iostream>
#include<algorithm>
#include<vector>
using namespace std;
int main() {
	vector<vector<int>> dp;
	for (int i = 0; i < 10; i++) {
		dp.resize(i);
		dp[i][0] = 1;
		dp[i][i] = 1;
	}
	for (int i = 0; i < )
}