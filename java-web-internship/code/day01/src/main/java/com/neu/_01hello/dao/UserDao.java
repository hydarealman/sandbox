package com.neu._01hello.dao;

/**
 * 用户 DAO 接口 (HelloWorld 示例用)
 * 对应 HelloWorldMapper.xml 中的 namespace
 */
public interface UserDao {
    // 方法名与 mapper XML 中的 id 对应
    // 这里只是一个标记接口，实际 SQL 由 HelloWorldMapper.xml 定义
}
